// The brand's single social account (facts.md §6). Handle is VERIFIED; the URL still owes a live
// click-test before cutover (owed-verification register #2). This is the ONLY social account — no
// TikTok/Facebook/X/YouTube. Do not add an icon for a profile that does not exist (facts.md §6).
//
// Lives here (plain constants, no server-only) so both the server-rendered footer and the drop banner
// can import it. Moved out of the deleted src/lib/demo.ts in 1.04.

export const INSTAGRAM_HANDLE = "@trajanovv2026";
export const INSTAGRAM_URL = "https://instagram.com/trajanovv2026";
